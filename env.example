# General project environment variables

# ---------------------------------------------------------------------
# DOCKER PERMISSIONS (LINUX ONLY)
# If you are on Linux, append your actual UID/GID to the end of .env
# file to avoid root-owned files in your workspace.
# (See README.md for the quick one-liner).
# ---------------------------------------------------------------------
# HOST_UID=1000
# HOST_GID=1000
